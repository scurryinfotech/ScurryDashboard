// This file previously contained duplicate helper types that conflicted with
// `DbHelper` and `SQLiteHelper` defined elsewhere in the project. Those
// implementations have been consolidated into `DbHelper.cs` and
// `SQLiteHelper.cs`. Keep this file empty or add non-conflicting helpers here.

namespace OrderService.Helpers
{
    // Intentionally left blank to avoid duplicate type definitions.
}
